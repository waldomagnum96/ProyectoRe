import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ReremakearmasPage } from './reremakearmas.page';

describe('ReremakearmasPage', () => {
  let component: ReremakearmasPage;
  let fixture: ComponentFixture<ReremakearmasPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ReremakearmasPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
