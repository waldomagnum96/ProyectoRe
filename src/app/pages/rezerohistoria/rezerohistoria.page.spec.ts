import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RezerohistoriaPage } from './rezerohistoria.page';

describe('RezerohistoriaPage', () => {
  let component: RezerohistoriaPage;
  let fixture: ComponentFixture<RezerohistoriaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RezerohistoriaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
