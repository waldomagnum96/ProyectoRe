import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ReremakepersoPage } from './reremakeperso.page';

describe('ReremakepersoPage', () => {
  let component: ReremakepersoPage;
  let fixture: ComponentFixture<ReremakepersoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ReremakepersoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
