import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ReremakehistoriaPage } from './reremakehistoria.page';

describe('ReremakehistoriaPage', () => {
  let component: ReremakehistoriaPage;
  let fixture: ComponentFixture<ReremakehistoriaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ReremakehistoriaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
