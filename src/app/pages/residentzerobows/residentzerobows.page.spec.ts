import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ResidentzerobowsPage } from './residentzerobows.page';

describe('ResidentzerobowsPage', () => {
  let component: ResidentzerobowsPage;
  let fixture: ComponentFixture<ResidentzerobowsPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ResidentzerobowsPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
